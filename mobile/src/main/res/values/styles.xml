<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">#c5c5c5</item>
        <item name="colorControlActivated">@color/offwhite</item>
        <item name="colorControlHighlight">@color/offwhite</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="RatingBarNew" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/start_rating_bar_full</item>
        <item name="android:minHeight">45dip</item>
        <item name="android:maxHeight">45dip</item>
    </style>
    <style name="RatingBarNewMid" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/star_rating_bar_full_mid</item>
        <item name="android:minHeight">45dip</item>
        <item name="android:maxHeight">45dip</item>
    </style>


</resources>
